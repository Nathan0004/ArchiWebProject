<!DOCTYPE html>

<html>
    <head>
        <title>Effectif Victorious Secrets</title>
        
    </head>
    <%- include('header.ejs') %>
    <body>
            
       
        
                <% listeusers.forEach(function(user) { %>
                         <%= user.username %> 
                <% }); %>
                
                </body>
                <%- include('footer.ejs') %>
     