<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Update Match </title>
</head>
<%- include('header.ejs') %>
<body>
    <h1>Match Update :  <%= id %></h1>
    <form action="/matchs/update" method="POST">
        <input type="hidden" value="<%= id %>" name="id">
        <label for="mtitre">Titre :</label>
        <input id="mtitre" type="text" name="titre" value ="<%= titre %>"><br>
        <label for="mdate">Date :</label>
        <input id="mdate" type="date" name="date" value ="<%= date %>"><br>
        <label for="mheure">Heure :</label>
        <input id="mheure" type="text" name="heure" value ="<%= heure %>"><br>
        <label for="madresse">Adresse :</label>
        <input id="madresse" type="adresse" name="adresse" value ="<%= adresse %>"><br>
        <SELECT name="coach" size="1">
            <OPTION>Coach</OPTION>
            <% listecoachs.forEach(function(coach) { %>

                <OPTION> <%= coach.lastname %> <%= coach.firstname[0] %> </OPTION>
             
             <% }); %> 
</SELECT>
    <SELECT name="AG" size="1">
                <OPTION>AG</OPTION>
                <% listejoueuses.forEach(function(joueuse) { %>

                    <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
                 
                 <% }); %> 
    </SELECT>

    <SELECT name="BU" size="1">
        <OPTION>BU</OPTION>
        <% listejoueuses.forEach(function(joueuse) { %>

            <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
         <% }); %> 
    </SELECT>

    <SELECT name="AD" size="1">
            <OPTION>AD</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>
  
 
    <SELECT name='MG' size="1">
            <OPTION>MG</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>

    <SELECT name="MC" size="1">
            <OPTION>MC</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>

    <SELECT name="MD" size="1">
            <OPTION>MD</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %>  
    </SELECT>
  
  
    <SELECT name="DG" size="1">
            <OPTION>DG</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>

    <SELECT name="DCG" size="1">
            <OPTION>DCG</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>

    <SELECT name="DCD" size="1">
            <OPTION>DCD</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT>

    <SELECT name="DD" size="1">
            <OPTION>DD</OPTION>
                <% listejoueuses.forEach(function(joueuse) { %>

                    <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
                 
                 <% }); %> 
    </SELECT>
  
  
    <SELECT name="G" size="1">
            <OPTION>G</OPTION>
            <% listejoueuses.forEach(function(joueuse) { %>

                <OPTION> <%= joueuse.lastname %> <%= joueuse.firstname[0] %> </OPTION>
             
             <% }); %> 
    </SELECT> </br>
        <input type="submit" name="save" value="Sauver">
       
        <a href="/matchs"> <input type="button" value="Retour"> </a>
    </form>

</body>
<%- include('footer.ejs') %>
</html>